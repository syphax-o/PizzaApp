/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PizzaIngredientColorPipe } from './pizzaIngredientColor.pipe';

describe('Pipe: PizzaIngredientColore', () => {
  it('create an instance', () => {
    let pipe = new PizzaIngredientColorPipe();
    expect(pipe).toBeTruthy();
  });
});
